{
  "name": "kart-recognition-service",
  "private": true,
  "type": "module",
  "scripts": {
    "smoke": "tsx scripts/smoke.ts",
    "eval": "tsx eval/run-eval.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "serve": "node --env-file-if-exists=.env.local node_modules/.bin/tsx scripts/serve.ts"
  },
  "dependencies": {
    "openai": "^6.10.0",
    "sharp": "^0.34.4",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
