OPENAI_API_KEY=sk-proj-replace-me

# Where the grounded enumerator runs. Optional: with this unset the census still names
# everything it can see and the shopper still gets a bag, it just has no outlines in it.
# See enumerator/README.md for what to deploy behind this URL.
ENUMERATOR_URL=
ENUMERATOR_TOKEN=

# Optional. Points the client at any OpenAI-compatible endpoint instead of OpenAI's
# own: Azure OpenAI, a second organisation, a gateway, or a locally served model. Leave
# it unset to use OpenAI. Useful when the usual account has no credit.
# OPENAI_BASE_URL=https://your-endpoint.example/v1
