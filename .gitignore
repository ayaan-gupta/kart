# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

example

# generated native folders
/android

# Superpowers SDD scratch
.superpowers/

# Offline pipeline runs. Contains corpus photographs with no licence manifest yet.
runs/
__pycache__/

# RPC eval images: CC BY-NC-SA, fetched on demand by fetch_rpc.py
server/eval/corpus/images/
server/eval/detection-score.json

# Local datasets, not redistributable
.data-grocer-help/
server/eval/catalog-score.json
server/eval/.cache/
server/eval/rerank-score.json
server/eval/probe-score-*.json
server/eval/finetune-score-*.json
server/eval/openset-score.json
server/eval/detection-sweep*.json

# Python eval environment (models + deps, never committed)
server/.venv/

# Cart and video corpora: fetched on demand under open licences, manifests are committed
server/eval/corpus/carts/
server/eval/corpus/videos/
server/eval/carts-frames.json
server/eval/carts-states.json
server/eval/video-frames.json
server/eval/video-states.json
server/eval/grocer-detection.json
server/eval/grocer-endtoend.json
